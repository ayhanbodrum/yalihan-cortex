use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * ⚠️ CONTEXT7 PERMANENT STANDARD:
     * - ALWAYS use 'status' field, NEVER use 'enabled'
     * - Pre-commit hook will BLOCK migrations with 'enabled'
     * - This is a PERMANENT STANDARD - NO EXCEPTIONS
     */
    public function up(): void
    {
        Schema::create('{{ table }}', function (Blueprint $table) {
            $table->id();

            // ✅ CONTEXT7 PERMANENT STANDARD: Status field MUST be TINYINT(1) boolean
            // ❌ NEVER use VARCHAR, ENUM, or Turkish values like 'Aktif'
            // ❌ NEVER use 'enabled' → This will be BLOCKED by pre-commit hook
            // Default: 1 (active), 0 (inactive)
            $table->tinyInteger('status')->default(1)->comment('0=inactive, 1=active (Context7 boolean - PERMANENT STANDARD)');

            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('{{ table }}');
    }
};
